# ignore all .exe files, which are the compiled c++ executables,
# *.exe this doesnt work in linux so we should ignore all other files other than cpp 
# (in ubuntu cpp output files do not have any extension):
*
!*.cpp 
# for java
*.class
# for python
*.py
#other binaries
*.so
*.dylib